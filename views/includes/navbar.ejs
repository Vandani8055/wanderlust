<nav class="navbar navbar-expand-md navbar-light bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand d-flex align-items-center" href="/listings">
      <img src="/images/brandlogo.png" alt="Wanderlust Logo" class="navbar-logo">
    </a>


    <div class="navbar-nav">
      <a class="nav-link" href="/listings">Explore</a>
    </div>





    <!-- wishlist -->
    <a href="/wishlist" class="nav-wishlist">
      <i class="fa-solid fa-heart"></i>
      <% if (currUser?.wishlist?.length) { %>
      <span class="wl-count"><%= currUser.wishlist.length %></span>
      <% } %>
    </a>





    <!-- search -->
    <div class="navbar-nav mx-auto">
      <form class="d-flex align-items-center" method="GET" action="/listings/search">
        <input 
    type="text" 
    name="query" 
    id="searchInput"
    class="form-control"
    placeholder="Search destinations..."
  />

  <!-- 🎤 VOICE BUTTON (NEW) -->
  <button type="button" id="voiceBtn" class="btn btn-outline-secondary ms-2" title="Voice Search">
    🎤
  </button>

  <button type="submit" class="btn btn-primary ms-2">Search</button>

      </form>
    </div>






    <div class="navbar-nav ms-auto align-items-center">
      <a class="nav-link" href="/listings/new">Airbnb Your Home</a>

      <% if (!currUser) { %>
      <a class="btn btn-login me-2" href="/login">Login</a>
      <a class="btn btn-signup" href="/signup">Sign Up</a>
      <% } %>

      <% if (currUser) { %>
      
      <a href="/profile" class="nav-link profile-nav">
        <img src="<%= currUser.profileImage || '/images/default-user.jpg' %>" 
         alt="Profile" 
         class="nav-profile-img">
      </a>

      <a class="btn btn-logout" href="/logout">Logout</a>
      <% } %>

    </div>
  </div>
</nav>



<style>
  .nav-profile-img {
  width: 38px;
  height: 38px;
  border-radius: 50%;
  object-fit: cover;
  border: 1px solid #ff385c;
  transition: transform 0.2s ease;
}

.nav-profile-img:hover {
  transform: scale(1.1);
}





.navbar-logo {
  height: 4.2rem;
  width: 4.5rem;
  margin-top: 0.5rem;
  border-radius: 50%;
  object-fit: cover;
  /* box-shadow: 0 2px 8px rgba(0,0,0,0.15); */
 
}
</style>









<script>
  const voiceBtn = document.getElementById("voiceBtn");
  const searchInput = document.getElementById("searchInput");

  const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;

  if (SpeechRecognition) {
    const recognition = new SpeechRecognition();
    recognition.lang = "en-US";

    voiceBtn.addEventListener("click", () => {
      recognition.start();
      voiceBtn.innerText = "🎙️";
    });

    recognition.onresult = (event) => {
      const spokenText = event.results[0][0].transcript;

      // ✅ THIS LINE CONNECTS VOICE TO YOUR EXISTING BACKEND
      searchInput.value = spokenText;

      // ✅ Auto-submit your current Fuse search
      searchInput.form.submit();
    };

    recognition.onend = () => {
      voiceBtn.innerText = "🎤";
    };
  }
</script>
