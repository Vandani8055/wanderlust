


<style>
    .font-manrope { font-family: 'Manrope', sans-serif; }
    
    .material-symbols-outlined { 
        font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24; 
    }

    /* Premium Indigo Gradient */
    .brand-gradient { 
        background: linear-gradient(135deg, #6366f1 0%, #a855f7 100%); 
    }
    
    .brand-text {
        background: linear-gradient(135deg, #4f46e5 0%, #9333ea 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    /* Glassmorphism for Mobile Nav */
    .glass-nav {
        background: rgba(255, 255, 255, 0.9);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
    }

    #searchInput:focus { outline: none; box-shadow: none; }
</style>

<nav class="sticky top-0 z-50 w-full bg-white/95 backdrop-blur-md border-b border-slate-200 shadow-sm font-manrope">
    <div class="max-w-[1440px] mx-auto px-4 sm:px-6 lg:px-10 h-16 flex items-center justify-between gap-4">
        
        <a href="/listings" class="flex items-center gap-2 shrink-0 no-underline group">
            <div class="size-9 sm:size-10 brand-gradient rounded-xl flex items-center justify-center text-white shadow-lg group-hover:scale-105 transition-transform">
                <span class="material-symbols-outlined text-[20px] sm:text-[24px]">explore</span>
            </div>
            <h1 class="text-xl font-extrabold tracking-tighter brand-text hidden md:block">Wanderlust</h1>
        </a>

        <div class="flex-1 flex items-center bg-slate-100 rounded-2xl px-4 py-2 max-w-[450px] border border-transparent focus-within:border-indigo-400 focus-within:bg-white focus-within:shadow-md transition-all group">
            <span class="material-symbols-outlined text-slate-400 group-focus-within:text-indigo-500 text-[20px] mr-2">search</span>
            <form action="/listings/search" method="GET" class="flex-grow flex items-center">
                <input 
                    name="q" 
                    id="searchInput" 
                    class="bg-transparent border-none p-0 text-sm w-full placeholder:text-slate-500 text-slate-800" 
                    placeholder="Search..." 
                    type="text"
                    required
                    autocomplete="off"
                />
                <button type="submit" class="hidden">Search</button>
            </form>
        </div>

        <div class="flex items-center gap-2 lg:gap-4 shrink-0">
            
            <a href="/wishlist" class="hidden sm:flex relative p-2 text-slate-600 hover:bg-slate-100 rounded-xl transition-colors">
                <span class="material-symbols-outlined text-[24px]">favorite</span>
                <% if (currUser?.wishlist?.length) { %>
                    <span class="absolute top-1 right-1 flex h-4 w-4 items-center justify-center rounded-full brand-gradient text-[9px] text-white font-bold border-2 border-white">
                        <%= currUser.wishlist.length %>
                    </span>
                <% } %>
            </a>

            <% if (currUser && currUser.role === "user") { %>
                <a href="/listings/new" class="hidden lg:block text-sm font-bold text-slate-700 hover:text-indigo-600">
                    Airbnb Your Home
                </a>
            <% } %>

            <div class="flex items-center gap-2 sm:pl-4 sm:border-l border-slate-200">
                <% if (!currUser) { %>
                    <a href="/login" class="text-sm font-bold text-slate-700 px-3 py-2 hover:bg-slate-100 rounded-xl hidden sm:block">Login</a>
                    <a href="/signup" class="brand-gradient text-white px-5 py-2 rounded-xl text-sm font-bold shadow-md hover:opacity-90 transition-all">
                        Sign Up
                    </a>
                <% } %>

                <% if (currUser) { %>
                    <div class="flex items-center gap-3">
                        <a href="/logout" class="hidden sm:block text-xs font-bold text-slate-400 hover:text-red-500 uppercase tracking-wider">Logout</a>
                        <a href="<%= currUser.role === 'admin' ? '/admin/dashboard' : '/dashboard' %>" 
                           class="size-9 rounded-full overflow-hidden border-2 border-indigo-100 hover:border-indigo-500 transition-all shadow-sm">
                            <img class="w-full h-full object-cover" 
                                 src="<%= currUser.profileImage ? currUser.profileImage : '/images/default.jpg' %>" 
                                 alt="Profile" />
                        </a>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</nav>

<div class="md:hidden fixed bottom-6 left-1/2 -translate-x-1/2 z-[100] w-[92%] max-w-[400px]">
    <div class="glass-nav border border-slate-200/50 px-6 py-3 flex items-center justify-between shadow-2xl rounded-3xl ring-1 ring-black/5">
        <a class="flex flex-col items-center gap-1 text-indigo-600" href="/listings">
            <span class="material-symbols-outlined text-[24px]">explore</span>
            <span class="text-[9px] font-bold uppercase">Explore</span>
        </a>
        <a class="flex flex-col items-center gap-1 text-slate-400" href="/wishlist">
            <span class="material-symbols-outlined text-[24px]">favorite</span>
            <span class="text-[9px] font-bold uppercase">Wishlist</span>
        </a>
        <% if (currUser) { %>
            <a class="flex flex-col items-center gap-1 text-slate-400" href="/dashboard">
                <span class="material-symbols-outlined text-[24px]">account_circle</span>
                <span class="text-[9px] font-bold uppercase">Profile</span>
            </a>
            <a class="flex flex-col items-center gap-1 text-slate-400" href="/logout">
                <span class="material-symbols-outlined text-[24px]">logout</span>
                <span class="text-[9px] font-bold uppercase">Exit</span>
            </a>
        <% } else { %>
            <a class="flex flex-col items-center gap-1 text-slate-400" href="/login">
                <span class="material-symbols-outlined text-[24px]">login</span>
                <span class="text-[9px] font-bold uppercase">Login</span>
            </a>
        <% } %>
    </div>
</div>